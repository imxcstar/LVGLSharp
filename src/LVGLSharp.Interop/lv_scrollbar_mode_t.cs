namespace LVGLSharp.Interop
{
    public enum lv_scrollbar_mode_t
    {
        LV_SCROLLBAR_MODE_OFF,
        LV_SCROLLBAR_MODE_ON,
        LV_SCROLLBAR_MODE_ACTIVE,
        LV_SCROLLBAR_MODE_AUTO,
    }
}
